/* ============================================================
   AUTODEV AGENCY — PORTFOLIO CONFIG
   ------------------------------------------------------------
   Supabase is OPTIONAL. Leave these two values empty and the
   portfolio runs entirely on the local content in data.js.
   To connect your own Supabase project later, just paste your
   own public URL + anon key here (never the service_role key).
============================================================ */

const SUPABASE_URL = "";
const SUPABASE_ANON_KEY = "";

/* ---- Company contact (used by the AI chat + contact form) ---- */
const AUTODEV_WHATSAPP = "213664415139";
const AUTODEV_EMAIL    = "contact@autodev.agency";
const AUTODEV_LOCATION = "Tizi Ouzou, Algérie";

/* Optional WhatsApp auto-notify (CallMeBot) — leave empty if unused */
const WHATSAPP_CALLMEBOT_APIKEY = "";
